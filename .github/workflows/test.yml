on: [push, pull_request]

name: test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: install
      run: npm ci
    - name: test
      run: npm run test
